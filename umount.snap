#!/bin/bash
x=1
  for variable in $(zfs list -t snapshot | grep -v NAME | awk -F " " {'print $1'});do
    arrVar+=("$variable")
    x=$((x+1))
  done
  for snaps in ${arrVar[@]};do
    umount -f /snaps/$snaps
  done

